# Copy menjadi .env untuk menjalankan backend PostgreSQL native lokal.
PORT=8787
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pt_smm_scada
DB_USER=postgres
DB_PASSWORD=ISI_PASSWORD_ANDA
# Gunakan true hanya bila server PostgreSQL mewajibkan SSL.
DATABASE_SSL=false
